import { DynamicComponentInfo } from './dynamic-component-info';

describe('DynamicComponentInfo', () => {
  it('should create an instance', () => {
    expect(new DynamicComponentInfo()).toBeTruthy();
  });
});
