import { AncorDirective } from './ancor.directive';

describe('AncorDirective', () => {
  it('should create an instance', () => {
    const directive = new AncorDirective();
    expect(directive).toBeTruthy();
  });
});
