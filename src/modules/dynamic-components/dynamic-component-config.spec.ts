import { DynamicComponentConfig } from './dynamic-component-config';

describe('DynamicComponentConfig', () => {
  it('should create an instance', () => {
    expect(new DynamicComponentConfig()).toBeTruthy();
  });
});
