import { PastDateValidatorDirective } from './past-date-validator.directive';

describe('PastDateValidatorDirective', () => {
  it('should create an instance', () => {
    const directive = new PastDateValidatorDirective();
    expect(directive).toBeTruthy();
  });
});
